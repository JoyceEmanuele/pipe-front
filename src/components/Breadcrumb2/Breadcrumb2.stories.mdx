<!-- Breadcrumb2.stories.mdx -->
import { ArgsTable, Meta, Story, Canvas} from '@storybook/addon-docs';

import { BrowserRouter, Route, Switch } from 'react-router-dom';
import { Breadcrumb2 } from '.';

<Meta title="Breadcrumb2" component={Breadcrumb2} />

export const Template = (args) => (
  <BrowserRouter>
    <Route>
      <Breadcrumb2 {...args}>Breadcrumb2</Breadcrumb2>
    </Route>
  </BrowserRouter>
);

# Breadcrumb2

A propriedade `link` é opcional.

<Canvas>
  <Story 
    name="default"
    args={{ 
      items: [
        { text: 'Cliente', link: '/#cliente' },
        { text: 'Unidade', link: '/#unidades' },
        { text: 'Máquinas' },
        { text: 'Máquina 1', link: '/#maquina1' },
      ],
    }}>
    {Template.bind({})}
  </Story>
</Canvas>
