<!-- AntSwitch.stories.mdx -->
import { ArgsTable, Meta, Story , Canvas} from '@storybook/addon-docs';

import { AntSwitch } from '.';

<Meta title="AntSwitch" component={AntSwitch} />

export const Template = (args) => (
      <AntSwitch {...args} />
);

# AntSwitch

<Canvas> 
  <Story 
    name="checked"
    args={{ 
      checked: true,
      label: 'AntSwitch',
    }}>
    {Template.bind({})}
  </Story>
  <Story 
    name="not checked"
    args={{ 
      checked: false,
      label: 'AntSwitch',
    }}>
    {Template.bind({})}
  </Story>
</Canvas>
