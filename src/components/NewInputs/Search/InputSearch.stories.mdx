import { ArgsTable, Meta, Story, Canvas } from "@storybook/addon-docs";
import { InputSearch } from ".";
import { BrowserRouter as Router } from 'react-router-dom';
import { Provider } from 'react-redux';
import store from 'store';

<Meta title="InputSearch" component={InputSearch} />

export const Template = (args) => (
  <Router>
    <Provider store={store}>
      <InputSearch {...args} />
    </Provider>
  </Router>
);

<Canvas> 
  <Story 
    name="InputSearch"
    args={{ label: "Search",
      placeholder: "Search...",
      name: "search",
      onChange:() => {}}}>
    {Template.bind({})}
  </Story>
  <Story 
    name="Disabled"
    args={{ label: "Search",
      placeholder: "Search...",
      name: "search",
      onChange:() => {},
      disabled: true
      }}>
    {Template.bind({})}
  </Story>
</Canvas>